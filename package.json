{
  "name": "osat",
  "version": "0.1.0",
  "private": true,
  "description": "OSAT — a calm, private layer over your Mac",
  "author": "Nate McCreery",
  "main": "desktop/main.cjs",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "start:mac": "npm run build && electron .",
    "dist:mac": "npm run build && electron-builder --mac dmg zip --arm64",
    "preview": "vite preview",
    "test": "node --test tests/*.test.mjs",
    "test:ui": "node tests/ui/smoke.mjs",
    "install:mac": "npm run dist:mac && node scripts/install-mac.mjs",
    "test:e2e": "node tests/e2e/electron.mjs",
    "ios": "node scripts/ios.mjs"
  },
  "dependencies": {
    "node-llama-cpp": "3.21.1",
    "node-pty": "^1.1.0"
  },
  "devDependencies": {
    "@phosphor-icons/react": "^2.1.10",
    "@vitejs/plugin-react": "5.0.4",
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/xterm": "^5.5.0",
    "electron": "43.2.0",
    "electron-builder": "26.15.3",
    "playwright": "1.56.1",
    "react": "19.2.0",
    "react-dom": "19.2.0",
    "vite": "6.4.2"
  },
  "build": {
    "appId": "ai.mccreery.osat",
    "productName": "OSAT",
    "asar": true,
    "directories": {
      "output": "release"
    },
    "files": [
      "dist/client/**/*",
      "desktop/**/*",
      "shared/**/*",
      "package.json",
      "!node_modules/node-llama-cpp/llama/gitRelease.bundle",
      "!node_modules/node-llama-cpp/llama/localBuilds/**/*",
      "!node_modules/node-llama-cpp/templates/**/*",
      "!node_modules/node-llama-cpp/bins/**/*"
    ],
    "mac": {
      "icon": "build/icon.icns",
      "target": [
        {
          "target": "dmg",
          "arch": [
            "arm64"
          ]
        },
        {
          "target": "zip",
          "arch": [
            "arm64"
          ]
        }
      ],
      "category": "public.app-category.productivity",
      "artifactName": "OSAT-${version}-${arch}-mac.${ext}",
      "extendInfo": {
        "NSAppleEventsUsageDescription": "OSAT shows what is playing in Spotify and lets you play, pause and skip.",
        "NSDesktopFolderUsageDescription": "OSAT shows the files on your Desktop, so you can open them or ask about them without leaving it.",
        "NSDocumentsFolderUsageDescription": "OSAT shows your Documents folder when you open it in Files, and finds files there from ⌘K.",
        "NSDownloadsFolderUsageDescription": "OSAT shows your Downloads folder when you open it in Files, and finds files there from ⌘K."
      },
      "hardenedRuntime": true,
      "entitlements": "build/entitlements.mac.plist",
      "entitlementsInherit": "build/entitlements.mac.plist"
    },
    "masDev": {
      "entitlements": "build/entitlements.mas.plist",
      "entitlementsInherit": "build/entitlements.mas.inherit.plist"
    },
    "asarUnpack": [
      "node_modules/node-pty/**",
      "shared/**",
      "node_modules/@node-llama-cpp/**"
    ],
    "dmg": {
      "title": "OSAT ${version}",
      "artifactName": "OSAT-${version}-${arch}.${ext}"
    }
  }
}
